#ifndef _READFILE_
#define _READFILE_
#include<iostream>
#include<fstream>
#include<string>
int * readFile(const std::string&s,const size_t& len);
//read the file and
//return the array
//len is the size of the array
#endif
